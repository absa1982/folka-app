Slim rounded progress bar for onboarding completion, week progress and quota usage.

```jsx
<ProgressBar value={3} max={8} showLabel />
<ProgressBar value={62} tone="accent" />
```

- `tone`: brand (default) · accent · warning · neutral.
- `showLabel` appends a mono `%`.
