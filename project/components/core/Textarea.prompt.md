Multi-line text input for notes, comments and descriptions; matches Input styling and shares the green focus ring.

```jsx
<Textarea rows={4} placeholder="Add an internal note about this candidate…" />
```

- `rows` sets initial height; user can drag-resize vertically.
- `invalid` for error state.
