Toggle switch for binary settings (notifications, app add-ons); brand green when on.

```jsx
<Switch checked={notify} onChange={setNotify} label="Weekly approval reminder" />
```

- Use for instant-apply settings; use Checkbox for form selections.
